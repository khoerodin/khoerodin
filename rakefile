def execute(command)
  system "#{command}"
end

desc 'Jekyll Serve'
task :s do
  execute("gulp && jekyll serve --watch --config _config.yml,_config_dev.yml")
end

desc 'Deploy to Surge'
task :deploy do
  execute("gulp && JEKYLL_ENV=production jekyll build && surge")
end
